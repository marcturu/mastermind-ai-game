$ make all: Compila i executa. Deixa tots els fitxers .class a /exe/out 

$ make driver: Compila i executa el Driver per provar el joc.

$ make executa_driver: Executa el Driver per provar el joc.

$ make allTest: Executa tots els tests de totes les classes.

$ make Test*nom_classe*: Executa el test de la classe en concret.

$ make clean: Borra tot el contingut de /EXE
